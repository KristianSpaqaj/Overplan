<Page
    x:Class="OverplanUWP.View.LeaderShiftsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OverplanUWP.View"
    xmlns:viewmodel="using:OverplanUWP.ViewModel"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodel:TestViewModel/>
    </Page.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="57*"/>
            <RowDefinition Height="943*"/>
        </Grid.RowDefinitions>


        <controls:DataGrid x:Name="dataGrid"
                           Height="300" Margin="300,293,300,350"
                           AutoGenerateColumns="True"
                           ItemsSource="{Binding getShiftoverview}" Grid.Row="1">

        </controls:DataGrid>
        <Button Command="{Binding getShift, Mode=OneWay}" Content="Hent Vagt" VerticalAlignment="Top" Grid.Row="1"/>


    </Grid>
</Page>
