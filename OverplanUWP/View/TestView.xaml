<Page
    x:Class="OverplanUWP.View.TestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OverplanUWP.View"
    xmlns:viewmodel="using:OverplanUWP.ViewModel"
    xmlns:data="using:OverplanUWP.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewmodel:TestViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="1" >
            <TextBox Text="{Binding navn, Mode=TwoWay}" FontSize="50"/>
            <TextBox Text="{Binding adresse, Mode=TwoWay}" FontSize="50"/>
            <TextBox Text="{Binding telefon, Mode=TwoWay}" FontSize="50"/>

            <TextBox Text="{Binding navn, Mode=TwoWay}" FontSize="50"/>
            <TextBox Text="{Binding adresse, Mode=TwoWay}" FontSize="50"/>
            <TextBox Text="{Binding telefon, Mode=TwoWay}" FontSize="50"/>
            <Button Command="{Binding AddMedarbejder, Mode=OneWay}" Content="Tilføj en ny medarbejder"/>
            <Button Command="{Binding AddVirksomhed, Mode=OneWay}" Content="Tilføj en Virksomhed"/>
            <Button Command="{Binding HentMedarbejder, Mode=OneWay}" Content="Hent Medarbejderplan"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.RowSpan="2">
            <Button Content="HELOo"/>
            <ListView ScrollViewer.VerticalScrollBarVisibility="Visible" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" ItemsSource="{Binding getMedarbejdersplan}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Navn}" FontSize="20"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
